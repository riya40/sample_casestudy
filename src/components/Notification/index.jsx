import React from "react";
import { Grid } from "@mui/material";

const Notification = ()=>{
    return(
        <Grid Container>
            <h1>
                this is home page
            </h1>
        </Grid>
    );
};

export default Notification;